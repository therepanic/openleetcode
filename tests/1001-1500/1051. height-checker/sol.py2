class Solution(object):
    def heightChecker(self, heights):
        """
        :type heights: List[int]
        :rtype: int
        """
        count = 0
        s = sorted(heights)
        for i, j in zip(heights, s):
            if i != j:
                count += 1
        return count
