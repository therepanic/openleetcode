class Solution(object):
    def minStartValue(self, nums):
        curr = ans = 0
        for i in nums:
            curr += i
            if curr < ans:
                ans = curr
        return max(ans, 1 - ans)
