class Solution(object):
    def numberOfSteps(self, num):
        """
        :type num: int
        :rtype: int
        """
        if num == 0:
            return 0
        steps = 0
        while num:
            steps += 1
            num = num // 2 if num % 2 == 0 else num - 1
        return steps
