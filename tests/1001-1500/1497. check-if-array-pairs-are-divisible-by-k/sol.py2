class Solution(object):
    def canArrange(self, arr, k):
        """
        :type arr: List[int]
        :type k: int
        :rtype: bool
        """
        a = sorted(v % k for v in arr)
        j = a.count(0)
        return j % 2 == 0 and sum((p + q) % k for p, q in zip(a[j:], a[::-1])) == 0
