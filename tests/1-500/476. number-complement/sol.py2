class Solution(object):
    def findComplement(self, num):
        """
        :type num: int
        :rtype: int
        """
        bit = 1
        curr = num
        while curr > 0:
            num ^= bit
            bit <<= 1
            curr >>= 1
        return num
