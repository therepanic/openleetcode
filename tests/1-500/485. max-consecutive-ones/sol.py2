class Solution(object):
    def findMaxConsecutiveOnes(self, nums):
        best = 0
        current = 0

        for num in nums:
            if num == 1:
                current += 1
                if current > best:
                    best = current
            else:
                current = 0

        return best
