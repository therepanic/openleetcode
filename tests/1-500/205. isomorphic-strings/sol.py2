class Solution(object):
    def isIsomorphic(self, s, t):
        st = {}
        ts = {}
        for i, (a, b) in enumerate(zip(s, t)):
            if st.get(a) != ts.get(b):
                return False
            st[a] = i
            ts[b] = i
        return True
