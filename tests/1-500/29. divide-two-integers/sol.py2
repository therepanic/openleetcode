class Solution:
    def divide(self, dividend, divisor):
        if dividend == -2 ** 31 and divisor == -1:
            return 2 ** 31 - 1
        ans = int(float(dividend) / float(divisor))
        return min(max(ans, -2 ** 31), 2 ** 31 - 1)
