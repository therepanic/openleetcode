class Solution:
    def longestCommonPrefix(self, strs):
        if not strs:
            return ''
        strs.sort()
        ans = ''
        i = 0
        while i < len(strs[0]) and strs[0][i] == strs[-1][i]:
            ans += strs[0][i]
            i += 1
        return ans
