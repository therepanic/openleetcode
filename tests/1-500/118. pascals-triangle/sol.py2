
class Solution(object):
    def generate(self, numRows):
        result = []
        for i in xrange(numRows):
            row = [1] * (i + 1)
            for j in xrange(1, i):
                row[j] = result[-1][j - 1] + result[-1][j]
            result.append(row)
        return result
