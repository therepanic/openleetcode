class Solution(object):
    def missingNumber(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        l = len(nums)
        total = l
        summ = 0
        for i in range(l):
            total += i
            summ += nums[i]
        return total - summ
