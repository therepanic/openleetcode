class Solution(object):
    def maximumGap(self, nums):
        if len(nums) < 2:
            return 0
        nums = sorted(nums)
        return max(nums[i] - nums[i - 1] for i in range(1, len(nums)))
