class Solution(object):
    def isOneBitCharacter(self, bits):
        """
        :type bits: List[int]
        :rtype: bool
        """
        n, i = len(bits), 0
        while i < n - 1:
            i += 1 + (bits[i] == 1)
        return i == n - 1
