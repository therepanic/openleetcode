class Solution:
    def minimumLengthEncoding(self, words):
        words = set(words)
        total = 0
        for word in list(words):
            if any(other != word and other.endswith(word) for other in words):
                continue
            total += len(word) + 1
        return total
