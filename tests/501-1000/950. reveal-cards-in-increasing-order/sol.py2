class Solution(object):
    def deckRevealedIncreasing(self, deck):
        deck.sort()
        rebuild = [deck[-1]]
        for card in reversed(deck[:-1]):
            rebuild.insert(0, rebuild.pop())
            rebuild.insert(0, card)
        return rebuild
