class Solution(object):
    def sortedSquares(self, nums):
        """
        :type nums: List[int]
        :rtype: List[int]
        """
        newarr = []
        for i in nums:
            newarr.append(i*i)
        newarr.sort()
        return newarr
