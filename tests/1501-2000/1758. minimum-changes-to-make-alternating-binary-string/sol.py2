class Solution(object):
    def minOperations(self, s):
        """
        :type s: str
        :rtype: int
        """
        c = 0
        j = 0
        n = len(s)
        for ch in s:
            if int(ch) == j:
                c += 1
            j ^= 1
        return min(c, n - c)
