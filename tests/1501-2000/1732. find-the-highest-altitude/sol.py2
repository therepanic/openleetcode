class Solution(object):
    def largestAltitude(self, gain):
        """
        :type gain: List[int]
        :rtype: int
        """
        ans = acc = 0
        for it in gain:
            acc += it
            d = acc - ans
            ans += d & ~(d >> 0x1F)
        return ans
