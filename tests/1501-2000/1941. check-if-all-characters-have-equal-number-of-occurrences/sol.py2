class Solution(object):
    def areOccurrencesEqual(self, s):
        """
        :type s: str
        :rtype: bool
        """
        d = {}
        for c in s:
            d[c] = d.get(c, 0) + 1
        return len(set(d.values())) == 1
