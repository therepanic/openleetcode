class Solution(object):
    def countGoodRectangles(self, rectangles):
        """
        :type rectangles: List[List[int]]
        :rtype: int
        """
        max_len = max(min(rec) for rec in rectangles)
        return sum(1 for rec in rectangles if min(rec) == max_len)
