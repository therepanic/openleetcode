class Solution(object):
    def minimumDeletions(self, s):
        """
        :type s: str
        :rtype: int
        """
        res = 0
        count = 0
        for ch in s:
            if ch == 'b':
                count += 1
            elif count:
                res += 1
                count -= 1
        return res
