class Solution(object):
    def trimMean(self, arr):
        """
        :type arr: List[int]
        :rtype: float
        """
        n = len(arr)
        per5 = int(0.05*n)
        arr.sort()
        arr = arr[per5:n-per5]
        return float(sum(arr))/(n-per5-per5)
